<div class="users-page">
  <h1 class="page-title">Trouver des amis</h1>

  <%= form_with url: users_path, method: :get, class: "user-search" do %>
    <%= text_field_tag :q, params[:q], placeholder: "Rechercher par nom…",
                       class: "user-search__input", autocomplete: "off" %>
  <% end %>

  <% if @users.empty? %>
    <p class="empty-state">Personne à afficher pour l'instant.</p>
  <% else %>
    <ul class="user-list">
      <% @users.each do |user| %>
        <li class="user-row">
          <%= link_to user_path(user), class: "user-row__main" do %>
            <span class="user-row__avatar"><%= avatar_for(user, size: 48) %></span>
            <span class="user-row__info">
              <span class="user-row__name"><%= user.display_name %></span>
              <% if user.top_genres.present? %>
                <span class="user-row__genres"><%= user.top_genres.first(3).join(" · ") %></span>
              <% end %>
            </span>
          <% end %>
          <%= render "users/friend_actions", user: user, friendship: current_user.friendship_with(user) %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
